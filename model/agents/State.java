package model.agents;

public enum State {
    searching,                      // agent is searching for a place of interest
    working,                        // agent is cleaning up a place of interest
    travelling                       // agent is traveling towards a place of interest
}
